class Solution {
    public static int largest(int[] arr) {
        int max=Integer.MIN_VALUE;
        int n=arr.length;
        for(int i=0;i<n;i++){
            if(max<arr[i]) max=arr[i];
        }
        return max;
    }
}
